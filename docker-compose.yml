services:
    event-bot:
        build:
            context: .
        container_name: discord_event_bot
        user: "1000:1000"
        restart: always
        env_file:
            - .env
        environment:
            TZ: Europe/Rome
        volumes:
            - ./data:/app/data
